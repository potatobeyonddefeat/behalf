{
  "name": "@behalfid/github-action",
  "version": "1.0.0",
  "description": "GitHub Action for BehalfID CI/CD permission verification gate",
  "private": true,
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map",
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^2.0.3"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "@vercel/ncc": "^0.45.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.0"
  },
  "overrides": {
    "undici": "^6.27.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
