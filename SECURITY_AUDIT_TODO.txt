# Files to review for a full security audit
# -------------------------------------------------
# - packages/sdk/src/webhooks.ts          (webhook signature verification)
# - examples/enforcement-demo/src/env.ts (environment variable handling)
# - components/ui/HomeDemo.tsx           (client-side rendering, data exposure)
# - lib/validation.ts                     (input validation, sanitization)
# - any files containing API keys, tokens, or private keys
# - build scripts and CI/CD configuration (e.g., .github/workflows/*)
# - server entry points (e.g., next.config.js, server.ts)
# - any custom encryption utilities
#
# Ensure each of these files:
#   • Does not contain hard-coded secrets.
#   • Retrieves secrets from process.env or a secret manager.
#   • Uses HTTPS/TLS for all external communication.
#   • Validates and sanitizes all external input.
#   • Logs no sensitive data in production.
#
# Last audit: 2026-06-18
# Result: PASSED
# - npm audit: 0 vulnerabilities (root, cli, sdk, examples, github-action)
#   - Patched nodemailer ^8.0.10 → ^9.0.1 (GHSA-p6gq-j5cr-w38f, high, SSRF/file-read via raw option)
# - Unit tests: 577/577 passed
# - No hardcoded secrets detected

# Last audit: 2026-06-20
# Result: PASSED
# - npm audit: 0 vulnerabilities (root, cli, sdk, examples, github-action)
#   - Patched undici 6.26.0 → 6.27.0 in packages/github-action (GHSA-p88m-4jfj-68fv, GHSA-vxpw-j846-p89q, GHSA-35p6-xmwp-9g52, GHSA-g8m3-5g58-fq7m — HTTP header injection, WebSocket DoS, response queue poisoning, SameSite downgrade)
# - Unit tests: 577/577 passed
# - No hardcoded secrets detected

# Last audit: 2026-06-21
# Result: PASSED
# - npm audit: 0 vulnerabilities (root, cli, sdk, examples/enforcement-demo, examples/node-demo, examples/webhook-receiver, examples/site-guard-express, examples/site-guard-nextjs, packages/github-action)
# - Unit tests: 577/577 passed
# - No hardcoded secrets detected

# Last audit: 2026-06-22
# Result: PASSED
# - npm audit: 0 vulnerabilities (root, cli, sdk, examples/enforcement-demo, examples/node-demo, examples/webhook-receiver, examples/site-guard-express, examples/site-guard-nextjs, packages/github-action)
# - Unit tests: 577/577 passed
# - No hardcoded secrets detected

# Last audit: 2026-06-23
# Result: PASSED
# - npm audit: 0 vulnerabilities (root, cli, sdk, examples/enforcement-demo, examples/node-demo, examples/webhook-receiver, examples/site-guard-express, examples/site-guard-nextjs, packages/github-action)
# - Unit tests: 581/581 passed
# - No hardcoded secrets detected

# Last audit: 2026-06-24
# Result: PASSED
# - npm audit: 0 vulnerabilities (root, cli, sdk, examples/enforcement-demo, examples/node-demo, examples/webhook-receiver, examples/site-guard-express, examples/site-guard-nextjs, packages/github-action)
# - Unit tests: 598/598 passed
# - No hardcoded secrets detected

# Last audit: 2026-06-25
# Result: PASSED
# - npm audit: 0 vulnerabilities (root, cli, sdk, examples/enforcement-demo, examples/node-demo, examples/webhook-receiver, examples/site-guard-express, examples/site-guard-nextjs, packages/github-action)
# - Unit tests: 598/598 passed
# - No hardcoded secrets detected

# Last audit: 2026-06-26
# Result: PASSED
# - npm audit: 0 vulnerabilities (root, cli, sdk, examples/enforcement-demo, examples/node-demo, examples/webhook-receiver, examples/site-guard-express, examples/site-guard-nextjs, packages/github-action)
# - Unit tests: 598/598 passed
# - No hardcoded secrets detected

# Last audit: 2026-06-27
# Result: PASSED
# - npm audit: 0 vulnerabilities (root, cli, sdk, examples/enforcement-demo, examples/node-demo, examples/webhook-receiver, examples/site-guard-express, examples/site-guard-nextjs, packages/github-action)
# - Unit tests: 598/598 passed
# - No hardcoded secrets detected

# Last audit: 2026-06-28
# Result: PASSED
# - npm audit: 0 vulnerabilities (root, cli, sdk, examples/enforcement-demo, examples/node-demo, examples/webhook-receiver, examples/site-guard-express, examples/site-guard-nextjs, packages/github-action)
# - Unit tests: 623/623 passed
# - No hardcoded secrets detected

# Last audit: 2026-06-29
# Result: PASSED
# - npm audit: 0 vulnerabilities (root, cli, sdk, examples/enforcement-demo, examples/node-demo, examples/webhook-receiver, examples/site-guard-express, examples/site-guard-nextjs, packages/github-action)
# - Unit tests: 625/625 passed
# - No hardcoded secrets detected

# Last audit: 2026-06-30
# Result: PASSED
# - npm audit: 0 vulnerabilities (root, cli, sdk, examples/enforcement-demo, examples/node-demo, examples/webhook-receiver, examples/site-guard-express, examples/site-guard-nextjs, packages/github-action)
# - Unit tests: 625/625 passed
# - No hardcoded secrets detected

# Last audit: 2026-07-24 (Task 1B production advisories)
# Result: PASSED
# - npm audit --omit=dev: 0 vulnerabilities (was 4 high + 2 moderate)
#   - next ^16.2.6 → ^16.2.11 (July 2026 security release)
#   - mongoose ^9.6.1 → ^9.7.2 (resolved 9.8.0; GHSA-664h-wqgq-64gw)
#   - overrides: postcss ^8.5.18, sharp ^0.35.0, undici ^6.27.0, @swc/helpers ^0.5.17
#   - @behalfid/github-action: @actions/core ^1.11.1 → ^2.0.3 (http-client 3.0.2 / undici 6.28.0)
# - Unit tests: 1606 passed, 6 skipped
# - No hardcoded secrets detected

# Last audit: 2026-07-28
# Result: PASSED
# - npm audit: 0 vulnerabilities (root, packages/cli, packages/sdk, packages/install,
#   packages/mcp-audit, packages/mcp-runtime, packages/egress-proxy, packages/github-action,
#   examples/enforcement-demo, examples/node-demo, examples/webhook-receiver,
#   examples/site-guard-express, examples/site-guard-nextjs)
# - Unit tests: 1613 passed, 78 skipped (root); 146 passed (egress-proxy, install, github-action)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - No hardcoded secrets detected (grepped for API key/AWS key/private-key/Slack-token patterns;
#   only committed env file is .env.example; no key/cert files tracked)

# Last audit: 2026-07-29
# Result: PASSED
# - npm audit (root + --omit=dev): 0 vulnerabilities
# - npm audit: 0 vulnerabilities in examples/enforcement-demo, examples/node-demo,
#   examples/site-guard-express, examples/site-guard-nextjs, examples/webhook-receiver
# - Unit tests: 1613 passed, 78 skipped (root); 146 passed (egress-proxy, install, github-action)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - No hardcoded secrets detected (grepped for API key/AWS key/private-key/Slack-token patterns;
#   only committed env file is .env.example; no key/cert files tracked)

# Last audit: 2026-07-30
# Result: PASSED
# - npm audit (root + --omit=dev): 0 vulnerabilities
# - npm audit: 0 vulnerabilities in examples/enforcement-demo, examples/node-demo,
#   examples/site-guard-express, examples/site-guard-nextjs, examples/webhook-receiver
# - Unit tests: 1687 passed, 78 skipped (root); 146 passed (egress-proxy, install, github-action)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - No hardcoded secrets detected (grepped for API key/AWS key/private-key/Slack-token patterns;
#   only committed env file is .env.example; no key/cert files tracked; one fake test fixture
#   key in test/mcp-audit.test.ts is intentional test data, not a real credential)

# Last audit: 2026-07-31
# Result: PASSED
# - npm audit (root + --omit=dev): 0 vulnerabilities
# - npm audit: 0 vulnerabilities in packages/cli, packages/github-action, packages/install,
#   packages/mcp-audit, packages/mcp-runtime, packages/sdk, examples/enforcement-demo,
#   examples/node-demo, examples/site-guard-express, examples/site-guard-nextjs,
#   examples/webhook-receiver
# - Unit tests: 1687 passed, 78 skipped (root); 146 passed (egress-proxy, install, github-action)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - No hardcoded secrets detected (grepped for API key/AWS key/private-key/Slack-token patterns;
#   only committed env file is .env.example / examples/site-guard-demo/.env.example; no key/cert
#   files tracked; the sk-... strings in test/mcp-audit.test.ts are intentional test fixtures,
#   not real credentials)

# Last audit: 2026-08-01
# Result: PASSED
# - npm audit (root + --omit=dev): 0 vulnerabilities
# - npm audit: 0 vulnerabilities in packages/cli, packages/github-action, packages/install,
#   packages/mcp-audit, packages/mcp-runtime, packages/sdk, packages/egress-proxy,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver
# - Unit tests: 1793 passed, 78 skipped (186/187 files)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - No hardcoded secrets detected (grepped for API key/AWS key/private-key/Slack-token patterns;
#   only committed env file is .env.example / examples/site-guard-demo/.env.example; no key/cert
#   files tracked)

# Last audit: 2026-08-02
# Result: PASSED
# - npm audit (root + --omit=dev): 0 vulnerabilities
# - npm audit: 0 vulnerabilities in packages/cli, packages/github-action, packages/install,
#   packages/mcp-audit, packages/mcp-runtime, packages/sdk, packages/egress-proxy,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver
# - Unit tests: 1828 passed, 78 skipped (193/194 files)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - No hardcoded secrets detected (grepped for API key/AWS key/private-key/Slack-token patterns;
#   only committed env file is .env.example / examples/site-guard-demo/.env.example; no key/cert
#   files tracked)

# Last audit: 2026-08-03
# Result: PASSED
# - npm audit (root + --omit=dev): 0 vulnerabilities
# - npm audit: 0 vulnerabilities in packages/cli, packages/github-action, packages/install,
#   packages/mcp-audit, packages/mcp-runtime, packages/sdk, packages/egress-proxy,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver
# - Unit tests: 1901 passed, 78 skipped (201/202 files, root); 146 passed (egress-proxy,
#   install, github-action)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - No hardcoded secrets detected (grepped for API key/AWS key/private-key/Slack-token patterns;
#   only committed env file is .env.example / examples/site-guard-demo/.env.example; no key/cert
#   files tracked)

# Last audit: 2026-08-04
# Result: PASSED (after remediation)
# - npm audit (root): found 2 high-severity advisories on install -
#   brace-expansion 5.0.8 (GHSA-rgw5-rvv9-x895, DoS bypassing prior CVE-2026-14257
#   mitigation) and fast-uri 3.1.4 (GHSA-7p8r-x3mc-p8w7, host confusion via
#   backslash authority introducer). Both are transitive build-time deps
#   (eslint -> minimatch -> brace-expansion; @sentry/nextjs -> webpack ->
#   schema-utils -> ajv -> fast-uri), not runtime request-path code. Fixed by
#   bumping the existing brace-expansion override to ^5.0.9 and adding a new
#   fast-uri override to ^3.1.5 in package.json; re-audit is now clean.
# - npm audit: 0 vulnerabilities in packages/cli, packages/github-action, packages/install,
#   packages/mcp-audit, packages/mcp-runtime, packages/sdk, packages/egress-proxy,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver
# - Unit tests: 1915 passed, 78 skipped (203/204 files, root); 146 passed (egress-proxy,
#   install, github-action); 93 passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - No hardcoded secrets detected (grepped for API key/AWS key/private-key/Slack-token patterns;
#   only committed env file is .env.example / examples/site-guard-demo/.env.example; no key/cert
#   files tracked; the sk-... strings in test/mcp-audit.test.ts are intentional test fixtures,
#   not real credentials)

# Last audit: 2026-08-05
# Result: PASSED
# - npm audit (root + --omit=dev): 0 vulnerabilities
# - npm audit: 0 vulnerabilities in packages/cli, packages/github-action, packages/install,
#   packages/mcp-audit, packages/mcp-runtime, packages/sdk, packages/egress-proxy,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver
# - Unit tests: 2038 passed, 78 skipped (210/211 files, root); 146 passed (egress-proxy,
#   install, github-action); 93 passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - No hardcoded secrets detected (grepped for API key/AWS key/private-key/Slack-token patterns;
#   only committed env file is .env.example / examples/site-guard-demo/.env.example; no key/cert
#   files tracked; the sk-... strings in test/mcp-audit.test.ts are intentional test fixtures,
#   not real credentials)

# Last audit: 2026-08-06
# Result: PASSED
# - npm audit (root + --omit=dev): 0 vulnerabilities
# - npm audit: 0 vulnerabilities in packages/cli, packages/github-action, packages/install,
#   packages/mcp-audit, packages/mcp-runtime, packages/sdk, packages/egress-proxy,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver
# - Unit tests: 2795 passed, 78 skipped (219/220 files, root); 146 passed (egress-proxy,
#   install, github-action); 93 passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - No hardcoded secrets detected (grepped for API key/AWS key/private-key/Slack-token patterns;
#   only committed env file is .env.example / examples/site-guard-demo/.env.example; no key/cert
#   files tracked; the sk-... strings in test/mcp-audit.test.ts are intentional test fixtures,
#   not real credentials)

# Last audit: 2026-08-07
# Result: PASSED (after remediation)
# - npm audit (root): found 1 high-severity advisory - js-yaml 4.3.0
#   (GHSA-5p4m-2wfm-xmqj, quadratic CPU consumption in !!omap resolution,
#   CVE-2026-59870 fix not backported to 4.x). Transitive build-time dep
#   (eslint -> @eslint/eslintrc -> js-yaml), not runtime request-path code.
#   npm audit --omit=dev was already 0 vulnerabilities. Fixed by adding a
#   js-yaml ^4.3.1 override in package.json; re-audit is now clean.
# - npm audit: 0 vulnerabilities in packages/cli, packages/github-action, packages/install,
#   packages/mcp-audit, packages/mcp-runtime, packages/sdk, packages/egress-proxy,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver
# - Unit tests: 2805 passed, 78 skipped (219/220 files, root); 146 passed (egress-proxy,
#   install, github-action); 93 passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - No hardcoded secrets detected (grepped for API key/AWS key/private-key/Slack-token/Google-API-key
#   patterns across the repo, including the newer example dirs without their own package.json;
#   only committed env file is .env.example / examples/site-guard-demo/.env.example; no key/cert
#   files tracked; the sk-... strings in test/mcp-audit.test.ts are intentional test fixtures,
#   not real credentials)

# Last audit: 2026-08-08
# Result: PASSED (after remediation)
# - npm audit (root): found 1 high-severity advisory - nanoid <3.3.17
#   (GHSA-2v37-7h3g-55p8, custom generators can loop indefinitely when size
#   is zero), a transitive dep via next -> postcss. Fixed with a
#   nanoid ^3.3.17 override in package.json; re-audit is now clean.
# - npm audit: 0 vulnerabilities in packages/cli, packages/github-action, packages/install,
#   packages/mcp-audit, packages/mcp-runtime, packages/sdk, packages/egress-proxy,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/webhook-receiver
# - examples/site-guard-nextjs: same nanoid advisory (own overrides block, not covered
#   by the root workspace); fixed with matching nanoid ^3.3.17 override; re-audit clean
# - Unit tests: 2805 passed, 78 skipped (219/220 files, root); 146 passed (egress-proxy,
#   install, github-action); 93 passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - No hardcoded secrets detected (grepped for AWS key/OpenAI key/Slack-token/Google-API-key/
#   private-key patterns across the repo; only committed env file is .env.example /
#   examples/site-guard-demo/.env.example; no key/cert files tracked; the sk-... string in
#   test/mcp-audit.test.ts is an intentional test fixture, not a real credential)

# Last audit: 2026-08-09
# Result: PASSED
# - npm audit (root + --omit=dev): 0 vulnerabilities
# - npm audit: 0 vulnerabilities in packages/cli, packages/github-action, packages/install,
#   packages/mcp-audit, packages/mcp-runtime, packages/sdk, packages/egress-proxy,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver
# - Unit tests: 2828 passed, 78 skipped (220/221 files, root); 146 passed (egress-proxy,
#   install, github-action); 93 passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - No hardcoded secrets detected (grepped for AWS key/OpenAI key/Slack-token/Google-API-key/
#   private-key patterns across the repo; only committed env file is .env.example /
#   examples/site-guard-demo/.env.example; no key/cert files tracked; the sk-... string in
#   test/mcp-audit.test.ts is an intentional test fixture, not a real credential)

# Last audit: 2026-08-10
# Result: PASSED
# - npm audit (root + --omit=dev): 0 vulnerabilities
# - npm audit: 0 vulnerabilities in packages/cli, packages/github-action, packages/install,
#   packages/mcp-audit, packages/mcp-runtime, packages/sdk, packages/egress-proxy,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver
# - Unit tests: 2828 passed, 78 skipped (220/221 files, root); 146 passed (egress-proxy,
#   install, github-action); 93 passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - No hardcoded secrets detected (grepped for API key/AWS key/private-key/Slack-token patterns;
#   only committed env file is .env.example / examples/site-guard-demo/.env.example; no key/cert
#   files tracked)

# Last audit: 2026-08-11
# Result: PASSED
# - npm audit (root + --omit=dev): 0 vulnerabilities
# - npm audit: 0 vulnerabilities in packages/cli, packages/github-action, packages/install,
#   packages/mcp-audit, packages/mcp-runtime, packages/sdk, examples/enforcement-demo,
#   examples/node-demo, examples/site-guard-express, examples/site-guard-nextjs,
#   examples/webhook-receiver
# - Unit tests: 2828 passed, 78 skipped (220/221 files, root); 146 passed (egress-proxy,
#   install, github-action); 93 passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - No hardcoded secrets detected (grepped for AWS key/OpenAI key/Slack-token/Google-API-key/
#   private-key patterns across the repo; only committed env file is .env.example /
#   examples/site-guard-demo/.env.example; no key/cert files tracked; the sk-... string in
#   test/mcp-audit.test.ts is an intentional test fixture, not a real credential)

# Last audit: 2026-08-12
# Result: PASSED
# - npm audit (root + --omit=dev): 0 vulnerabilities
# - npm audit: 0 vulnerabilities in packages/cli, packages/github-action, packages/install,
#   packages/mcp-audit, packages/mcp-runtime, packages/sdk, packages/egress-proxy,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver
# - Unit tests: 2835 passed, 78 skipped (221/222 files, root); 146 passed (egress-proxy,
#   install, github-action); 93 passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - No hardcoded secrets detected (grepped for AWS key/sk-.../Slack-token/Google-API-key/
#   private-key patterns across the repo; only committed env file is .env.example /
#   examples/site-guard-demo/.env.example; no key/cert files tracked; the sk-... string in
#   test/mcp-audit.test.ts is an intentional test fixture, not a real credential)

# Last audit: 2026-08-13
# Result: PASSED
# - npm audit (root + --omit=dev): 0 vulnerabilities
# - npm audit: 0 vulnerabilities in packages/cli, packages/github-action, packages/install,
#   packages/mcp-audit, packages/mcp-runtime, packages/sdk, packages/egress-proxy,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver
# - Unit tests: 2835 passed, 78 skipped (221/222 files, root); 146 passed (egress-proxy,
#   install, github-action); 93 passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - No hardcoded secrets detected (grepped for AWS key/sk-.../Slack-token/Google-API-key/
#   private-key patterns across the repo; only committed env file is .env.example /
#   examples/site-guard-demo/.env.example; no key/cert files tracked; the sk-... string in
#   test/mcp-audit.test.ts is an intentional test fixture, not a real credential)

# Last audit: 2026-08-15
# Result: PASSED
# - npm audit (root + --omit=dev): 0 vulnerabilities
# - npm audit: 0 vulnerabilities in packages/cli, packages/github-action, packages/install,
#   packages/mcp-audit, packages/mcp-runtime, packages/sdk, packages/egress-proxy,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver
# - Unit tests: 2839 passed, 78 skipped (222/223 files, root); 146 passed (egress-proxy,
#   install, github-action); 93 passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - No hardcoded secrets detected (grepped for AWS key/sk-.../Slack-token/Google-API-key/
#   private-key patterns across the repo; only committed env file is .env.example /
#   examples/site-guard-demo/.env.example; no key/cert files tracked; the sk-... string in
#   test/mcp-audit.test.ts is an intentional test fixture, not a real credential)

# Last audit: 2026-08-16
# Result: PASSED
# - npm audit (root + --omit=dev): 0 vulnerabilities
# - npm audit: 0 vulnerabilities in packages/cli, packages/egress-proxy, packages/github-action,
#   packages/install, packages/mcp-audit, packages/mcp-runtime, packages/sdk,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver
# - Unit tests: 2839 passed, 78 skipped (222/223 files, root); 146 passed (egress-proxy,
#   install, github-action); 93 passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - No hardcoded secrets detected (grepped for AWS key/sk-.../Slack-token/Google-API-key/
#   private-key patterns across the repo; only committed env file is .env.example /
#   examples/site-guard-demo/.env.example; no key/cert files tracked; the sk-... string in
#   test/mcp-audit.test.ts is an intentional test fixture, not a real credential)

# Last audit: 2026-08-17
# Result: PASSED
# - npm audit (root + --omit=dev): 0 vulnerabilities
# - npm audit: 0 vulnerabilities in packages/cli, packages/egress-proxy, packages/github-action,
#   packages/install, packages/mcp-audit, packages/mcp-runtime, packages/sdk,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver
# - Unit tests: 2839 passed, 78 skipped (222/223 files, root); 146 passed (egress-proxy,
#   install, github-action); 93 passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - No hardcoded secrets detected (grepped for AWS key/sk-.../Slack-token/Google-API-key/
#   private-key patterns across the repo, excluding compiled dist/ bundles; only committed
#   env file is .env.example / examples/site-guard-demo/.env.example; no key/cert files
#   tracked; the sk-... string in test/mcp-audit.test.ts is an intentional test fixture,
#   not a real credential)

# Last audit: 2026-08-18
# Result: PASSED
# - npm audit (root + --omit=dev): 0 vulnerabilities
# - npm audit: 0 vulnerabilities in packages/cli, packages/egress-proxy, packages/github-action,
#   packages/install, packages/mcp-audit, packages/mcp-runtime, packages/sdk,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver
# - Unit tests: 2839 passed, 78 skipped (222/223 files, root); 146 passed (egress-proxy,
#   install, github-action); 93 passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - No hardcoded secrets detected (grepped for AWS key/sk-.../Slack-token/Google-API-key/
#   private-key patterns across the repo, excluding compiled dist/ bundles; only committed
#   env file is .env.example / examples/site-guard-demo/.env.example; no key/cert files
#   tracked; the sk-... string in test/mcp-audit.test.ts is an intentional test fixture,
#   not a real credential)

# Last audit: 2026-08-19
# Result: PASSED
# - npm audit (root + --omit=dev): 0 vulnerabilities
# - npm audit: 0 vulnerabilities in packages/cli, packages/egress-proxy, packages/github-action,
#   packages/install, packages/mcp-audit, packages/mcp-runtime, packages/sdk,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver
# - Unit tests: 2839 passed, 78 skipped (222/223 files, root); 146 passed (egress-proxy,
#   install, github-action); 93 passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - No hardcoded secrets detected (grepped for AWS key/sk-.../Slack-token/Google-API-key/
#   private-key patterns across the repo, excluding compiled dist/ bundles; only committed
#   env file is .env.example / examples/site-guard-demo/.env.example; no key/cert files
#   tracked; the sk-... string in test/mcp-audit.test.ts is an intentional test fixture,
#   not a real credential)

# Last audit: 2026-08-20
# Result: PASSED
# - npm audit (root + --omit=dev): 0 vulnerabilities
# - npm audit: 0 vulnerabilities in packages/cli, packages/egress-proxy, packages/github-action,
#   packages/install, packages/mcp-audit, packages/mcp-runtime, packages/sdk,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver
# - Unit tests: 2839 passed, 78 skipped (222/223 files, root); 146 passed (egress-proxy,
#   install, github-action); 93 passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - npx tsc --noEmit: clean
# - No hardcoded secrets detected (grepped for AWS key/sk-.../Slack-token/Google-API-key/
#   private-key patterns across the repo, excluding compiled dist/ bundles; only committed
#   env file is .env.example / examples/site-guard-demo/.env.example; no key/cert files
#   tracked; the sk-... string in test/mcp-audit.test.ts is an intentional test fixture,
#   not a real credential)

# Last audit: 2026-08-21
# Result: PASSED
# - npm audit (root + --omit=dev): 0 vulnerabilities
# - npm audit: 0 vulnerabilities in packages/cli, packages/egress-proxy, packages/github-action,
#   packages/install, packages/mcp-audit, packages/mcp-runtime, packages/sdk,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver
# - Unit tests: 2839 passed, 78 skipped (222/223 files, root); 146 passed (egress-proxy,
#   install, github-action); 93 passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - npx tsc --noEmit: clean
# - No hardcoded secrets detected (grepped for AWS key/sk-.../Slack-token/Google-API-key/
#   private-key patterns across the repo, excluding compiled dist/ bundles; only committed
#   env file is .env.example / examples/site-guard-demo/.env.example; no key/cert files
#   tracked; the sk-... string in test/mcp-audit.test.ts is an intentional test fixture,
#   not a real credential)

# Last audit: 2026-08-21 (deep authz/business-logic + compliance review)
# Result: PASSED (1 confirmed Medium finding fixed)
# - FIXED: disabled agents could still obtain a signed egress ticket via
#   POST /api/egress/authorize for the platform host and any host in
#   BEHALFID_EGRESS_ALLOW_HOSTS, because those two allow-branches in
#   authorizeEgressRequest() (lib/egressAuthorize.ts) returned allowed:true
#   before ever reaching verifyAction(), which is the only place that checked
#   agentStatus === "disabled". Every other enforcement path (/api/verify,
#   /api/gateway/execute, /api/actions/execute, agent branch of
#   /api/permissions) already threads agentStatus into verifyAction() and was
#   unaffected. Added an explicit disabled-agent check at the top of
#   authorizeEgressRequest() so it applies uniformly, plus two regression
#   tests in test/egress-proxy.test.ts (platform-host and allowlist branches)
#   that fail against the pre-fix code and pass after.
# - Reviewed and cleared (no issues found): IDOR/tenant scoping across all
#   app/api/dashboard/** and app/api/console/** routes; "agent cannot grant
#   permissions" server-side enforcement; self-approval / role-escalation
#   guards in lib/delegatedAuth.ts; console/dashboard mutation Origin checks;
#   Stripe + Slack + SDK webhook signature verification (fail-closed,
#   constant-time, replay-window bounded); atomicity of approval
#   approve/deny and grant-consumption updates; durability of the privileged-
#   mutation audit trail (webhook_events); admin/setup-token timing-safe
#   comparison and unsafe-default-password blocking in lib/adminAuth.ts;
#   fail-closed deny precedence in lib/verify.ts; SSRF protections in
#   lib/ssrf.ts / lib/actionGateway.ts (private/internal IP + DNS-rebinding
#   defenses, per-redirect-hop re-validation).
# - Compliance: docs/compliance/GAP_ANALYSIS.md already tracks the residual
#   non-code items (ISMS policy approval, backup/DR drills, vendor DPAs for
#   Supabase/HeyCatch, CSRF-token hardening, API-key-hash pepper) as
#   Medium/Low hardening or process gaps, not confirmed vulnerabilities; no
#   new compliance-relevant code defect found. Public /privacy and
#   /compliance pages remain in sync with the Postgres/Supabase + HeyCatch
#   architecture (no stale MongoDB references).
# - npm audit (root + --omit=dev): 0 vulnerabilities
# - npm audit: 0 vulnerabilities in packages/cli, packages/egress-proxy, packages/github-action,
#   packages/install, packages/mcp-audit, packages/mcp-runtime, packages/sdk,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver
# - Unit tests: 2841 passed, 78 skipped (223 files, root, incl. 2 new egress
#   regression tests); 146 passed (egress-proxy, install, github-action); 93
#   passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - npx tsc --noEmit: clean
# - npm run build: clean (Next.js production build succeeded)
# - npm run lint: pre-existing errors only (121 errors / 75 warnings, all in
#   already-committed code/tests, unchanged by this run); CI does not gate on
#   lint per AGENTS.md
# - No hardcoded secrets detected (grepped for AWS key/sk-.../Slack-token/Google-API-key/
#   private-key patterns across the repo, excluding compiled dist/ bundles; only committed
#   env file is .env.example / examples/site-guard-demo/.env.example; no key/cert files
#   tracked; the sk-... string in test/mcp-audit.test.ts and test/cli-scan.test.ts is an
#   intentional test fixture, not a real credential)

# Last audit: 2026-08-22 (full security + compliance audit)
# Result: PASSED (no new confirmed findings; prior Medium fix verified)
# - Verified the 2026-08-21 disabled-agent egress-authorization fix
#   (lib/egressAuthorize.ts) is in place and that all four verifyAction /
#   authorizeEgressRequest entry points (app/api/verify, app/api/gateway/execute,
#   app/api/actions/execute, app/api/egress/authorize) thread agentStatus
#   consistently.
# - Reviewed CSP/nonce middleware (proxy.ts), SSRF guards (lib/ssrf.ts),
#   production env validation (lib/env.ts), and the Redis rate-limit fallback
#   (lib/rateLimit.ts) — all consistent with the previously-documented mature
#   state; no confirmed defects.
# - HARDENING NOTE (not a confirmed vulnerability): lib/egressAuthorize.ts's
#   ticketSecret() falls back to a hardcoded string
#   ("behalfid-dev-egress-ticket-secret") if BEHALFID_EGRESS_TICKET_SECRET,
#   BEHALFID_WEBHOOK_SIGNING_PEPPER, and NEXTAUTH_SECRET are all unset, and
#   that env var isn't in lib/env.ts's REQUIRED_PRODUCTION_ENV_BASE. Currently
#   non-exploitable: verifyEgressTicket() has no caller outside its own module
#   and tests, so no code path trusts a ticket's signature as an authorization
#   decision — the security boundary is the authorizeEgressRequest() call
#   itself, not the ticket. Recommend either wiring ticket verification into
#   the actual egress path or requiring BEHALFID_EGRESS_TICKET_SECRET in
#   production if that's planned; tracking here rather than changing
#   behavior speculatively.
# - npm audit (root, --omit=dev and default) + all workspaces/examples
#   (packages/cli, packages/egress-proxy, packages/github-action,
#   packages/install, packages/mcp-audit, packages/mcp-runtime, packages/sdk,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver): 0 vulnerabilities
# - Unit tests: 2841 passed, 78 skipped (222/223 files, root); 146 passed
#   (egress-proxy, install, github-action); 93 passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - npx tsc --noEmit: clean
# - npm run build: clean (Next.js production build succeeded)
# - npm run lint: pre-existing errors only, unchanged by this run; CI does not
#   gate on lint per AGENTS.md
# - No hardcoded secrets detected (same grep methodology as prior audits;
#   only committed env file is .env.example / examples/site-guard-demo/.env.example;
#   no key/cert files tracked; sk-... strings in test fixtures are intentional)

# Last audit: 2026-08-23 (full security + compliance audit)
# Result: PASSED (no new confirmed findings)
# - Two independent focused reviews (auth/authorization/IDOR; input handling/
#   injection/SSRF/webhooks) re-derived findings directly from source rather
#   than trusting this log's prior entries, per the 2026-08-22 review's own
#   recommendation. Both concluded no currently-exploitable issue:
#   - Session/tenant scope is always re-derived server-side
#     (getWorkspaceActor/requireDeveloperApi/authenticateAgent); mutation
#     routes under app/api/dashboard/** and app/api/console/** filter by
#     accountId/developerUserId derived from the authenticated session, not
#     from client-supplied IDs. Self-approval is explicitly blocked in
#     lib/delegatedAuth.ts (canApproveRequest). Workspace slug header is
#     stripped from inbound requests in proxy.ts and only re-set after
#     proxy-side path matching, so it is not client-forgeable.
#   - SQL injection: all lib/repositories/postgres/*.ts queries use Drizzle's
#     parameterized sql`` templates; the only sql.raw() use (sqlInList) takes
#     hardcoded enum constants, never runtime input.
#   - SSRF: lib/ssrf.ts / lib/actionGateway.ts block private/internal/
#     metadata IP ranges, re-validate on every redirect hop, and pin the TCP
#     connection to the pre-resolved IP (DNS-rebinding safe).
#   - Open redirect: OAuth callback and logout next= params are validated by
#     safeOAuthNextPath()/resolveOwnedHref() against a same-origin/allowlist
#     check.
#   - Webhook signature verification (SDK, Stripe, Slack) remains fail-closed
#     and timing-safe.
#   - Re-verified the 2026-08-21 disabled-agent egress-authorization fix
#     (lib/egressAuthorize.ts) is still in place and applied uniformly.
# - No hardcoded secrets, private keys, or embedded-credential connection
#   strings found (grepped tracked source for API-key/token/password/secret
#   assignment patterns, AWS key IDs, PEM private-key headers, and
#   protocol://user:pass@ connection strings); only committed env file is
#   .env.example / examples/site-guard-demo/.env.example.
# - GitHub Actions: no pull_request_target usage; contents:read (or minimal
#   write for release workflows) permissions blocks present on all workflows.
# - npm audit (root, --omit=dev and default) + all workspaces/examples
#   (packages/cli, packages/egress-proxy, packages/github-action,
#   packages/install, packages/mcp-audit, packages/mcp-runtime, packages/sdk,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver): 0 vulnerabilities
# - Unit tests: 2847 passed, 78 skipped (223/224 files, root); 130 passed
#   (cli, egress-proxy, install combined); 14 passed (github-action)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - npx tsc --noEmit: clean
# - npm run build: clean (Next.js production build succeeded)
# - npm run lint: pre-existing errors only (121 errors / 75 warnings, all in
#   already-committed code/tests, unchanged by this run); CI does not gate on
#   lint per AGENTS.md
# - Compliance: docs/compliance/GAP_ANALYSIS.md gaps (G-01..G-27) reviewed;
#   no new code-level defect found affecting any tracked item. Remaining
#   open items there are organizational/process gaps (ISMS approval, backup
#   restore drill, vendor DPAs, deploy change-control) or already-documented
#   hardening recommendations (rate-limit Redis fallback, CSRF token,
#   egress-ticket secret default) outside this run's safe-remediation scope.

# Last audit: 2026-08-24 (full security + compliance audit)
# Result: PASSED (no new confirmed findings; no code changes since 2026-08-23)
# - No commits landed between the 2026-08-23 deep audit and this run, so this
#   pass re-validated the existing state rather than reviewing new code:
#   re-verified the 2026-08-21 disabled-agent egress-authorization fix
#   (lib/egressAuthorize.ts: agentStatus === "disabled" check at the top of
#   authorizeEgressRequest()) is still in place; re-checked GitHub Actions
#   workflows (no pull_request_target usage; all four workflows carry
#   explicit permissions blocks); re-reviewed docs/compliance/GAP_ANALYSIS.md
#   — all open items (G-06/G-07/G-08/G-09 organizational/ISMS gaps, G-10
#   API-key pepper, G-12 deploy change-control, G-13 webhook transactionality,
#   G-14 queue worker, G-15 rate-limit Redis fallback, G-16/G-27 vendor DPAs,
#   G-17 CSRF token, G-18..G-20 hardening notes) remain accurately tracked as
#   organizational/process gaps or hardening recommendations, not confirmed
#   code vulnerabilities requiring a fix.
# - npm audit (root, --omit=dev and default) + all workspaces/examples
#   (packages/cli, packages/egress-proxy, packages/github-action,
#   packages/install, packages/mcp-audit, packages/mcp-runtime, packages/sdk,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver): 0 vulnerabilities
# - Unit tests: 2847 passed, 78 skipped (223/224 files, root); 146 passed
#   (egress-proxy, install, github-action combined); 93 passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - npx tsc --noEmit: clean
# - npm run build: clean (Next.js production build succeeded)
# - npm run lint: pre-existing errors only (121 errors / 75 warnings, all in
#   already-committed code/tests, unchanged by this run); CI does not gate on
#   lint per AGENTS.md
# - No hardcoded secrets, private keys, or embedded-credential connection
#   strings found (grepped tracked source for API-key/token/password/secret
#   assignment patterns, AWS key IDs, PEM private-key headers, and
#   protocol://user:pass@ connection strings); only committed env file is
#   .env.example / examples/site-guard-demo/.env.example; sk-... string in
#   test/mcp-audit.test.ts remains an intentional test fixture.

# Last audit: 2026-08-25 (full security + compliance audit)
# Result: PASSED (no new confirmed findings; no code changes since 2026-08-24)
# - No commits landed between the 2026-08-24 audit and this run (HEAD unchanged
#   at dc40404), so this pass re-validated the existing state from source
#   rather than reviewing a diff: re-verified the 2026-08-21 disabled-agent
#   egress-authorization fix (lib/egressAuthorize.ts: agentStatus === "disabled"
#   check at the top of authorizeEgressRequest(), and all four verifyAction /
#   authorizeEgressRequest entry points — app/api/verify, app/api/gateway/execute,
#   app/api/actions/execute, app/api/egress/authorize — still thread agentStatus
#   consistently); re-checked GitHub Actions workflows (no pull_request_target
#   usage; all four workflows carry explicit permissions blocks); re-read
#   packages/sdk/src/webhooks.ts (HMAC verification remains fail-closed and
#   timing-safe); re-reviewed docs/compliance/GAP_ANALYSIS.md — all open items
#   (G-06 through G-27, e.g. ISMS/org gaps, backup restore drill, vendor DPAs,
#   API-key pepper, CSRF token, rate-limit Redis memory fallback) remain
#   accurately tracked as organizational/process gaps or already-documented
#   hardening recommendations, not confirmed code vulnerabilities requiring a
#   fix in this run.
# - npm audit (root, --omit=dev and default) + all workspaces/examples
#   (packages/cli, packages/egress-proxy, packages/github-action,
#   packages/install, packages/mcp-audit, packages/mcp-runtime, packages/sdk,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver): 0 vulnerabilities
# - Unit tests: 2847 passed, 78 skipped (223/224 files, root); 130 passed
#   (cli, egress-proxy, install combined); 14 passed (github-action); 93
#   passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - npx tsc --noEmit: clean
# - npm run build: clean (Next.js production build succeeded)
# - npm run lint: pre-existing errors only (121 errors / 75 warnings, all in
#   already-committed code/tests, unchanged by this run); CI does not gate on
#   lint per AGENTS.md
# - No hardcoded secrets, private keys, or embedded-credential connection
#   strings found (grepped tracked source for API-key/token/password/secret
#   assignment patterns, AWS key IDs, PEM private-key headers, and
#   protocol://user:pass@ connection strings, excluding compiled dist/
#   bundles); only committed env file is .env.example /
#   examples/site-guard-demo/.env.example; sk-... string in
#   test/mcp-audit.test.ts remains an intentional test fixture.

# Last audit: 2026-08-27 (full security + compliance audit)
# Result: PASSED (no new confirmed findings; no code changes since 2026-08-25)
# - No commits landed between the 2026-08-25 audit and this run (HEAD unchanged
#   at 8629f31), so this pass re-validated the existing state from source
#   rather than reviewing a diff: re-verified the disabled-agent
#   egress-authorization fix (lib/egressAuthorize.ts: agentStatus === "disabled"
#   check at the top of authorizeEgressRequest()) and that all five
#   verifyAction / authorizeEgressRequest entry points — app/api/verify,
#   app/api/gateway/execute, app/api/actions/execute, app/api/egress/authorize,
#   app/api/passport/[agentId] — still thread agentStatus consistently;
#   re-checked GitHub Actions workflows (no pull_request_target usage; all
#   four workflows carry explicit permissions blocks); re-read
#   packages/sdk/src/webhooks.ts (HMAC verification remains fail-closed and
#   timing-safe); re-checked security headers (next.config.ts: HSTS,
#   X-Frame-Options: DENY, CSP via middleware.ts) and session cookie flags
#   (httpOnly always, secure in production) remain intact; re-reviewed
#   docs/compliance/GAP_ANALYSIS.md — all open items (G-06 through G-27, e.g.
#   ISMS/org gaps, backup restore drill, vendor DPAs, API-key pepper, CSRF
#   token, rate-limit Redis memory fallback) remain accurately tracked as
#   organizational/process gaps or already-documented hardening
#   recommendations, not confirmed code vulnerabilities requiring a fix in
#   this run.
# - npm audit (root, default and --omit=dev) + all workspaces/examples
#   (packages/cli, packages/egress-proxy, packages/github-action,
#   packages/install, packages/mcp-audit, packages/mcp-runtime, packages/sdk,
#   examples/enforcement-demo, examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver): 0 vulnerabilities
# - Unit tests: 2847 passed, 78 skipped (223/224 files, root); 146 passed
#   (egress-proxy, install, github-action combined); 93 passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute: MongoMemoryServer
#     binary download blocked by this sandbox's outbound network policy (fastdl.mongodb.org);
#     unrelated to source changes and runs normally in GitHub Actions CI
# - npx tsc --noEmit: clean
# - npm run build: clean (Next.js production build succeeded, local disposable
#   Postgres 16 with drizzle-kit migrate applied)
# - npm run lint: pre-existing errors only (121 errors / 75 warnings, all in
#   already-committed code/tests, unchanged by this run); CI does not gate on
#   lint per AGENTS.md
# - No hardcoded secrets, private keys, or embedded-credential connection
#   strings found (grepped tracked source for API-key/token/password/secret
#   assignment patterns, AWS key IDs, PEM private-key headers, and
#   protocol://user:pass@ connection strings, excluding compiled dist/
#   bundles); only committed env file is .env.example /
#   examples/site-guard-demo/.env.example; sk-... string in
#   test/mcp-audit.test.ts remains an intentional test fixture; the two
#   protocol://user:pass@ matches found (.github/workflows/ci.yml,
#   scripts/dev/authenticated-shell-preview.mjs) are both local/CI-only
#   default credentials (postgres:postgres@localhost,
#   behalf:behalf@localhost), not real secrets.

# Last audit: 2026-08-28 (full security + compliance audit)
# Result: PASSED (no new confirmed findings; no code changes since 2026-08-25
#   / HEAD unchanged at 7e64137 other than prior nightly-audit log commits)
# - No commits landed with source changes since the 84e6c33 "robots.txt sync"
#   commit reviewed in prior audits, so this pass re-validated the existing
#   state from source rather than reviewing a diff: re-verified the
#   disabled-agent egress-authorization fix (lib/egressAuthorize.ts:
#   agentStatus === "disabled" check at the top of authorizeEgressRequest())
#   and that all five verifyAction / authorizeEgressRequest entry points —
#   app/api/verify, app/api/gateway/execute, app/api/actions/execute,
#   app/api/egress/authorize, app/api/passport/[agentId] — still thread
#   agentStatus consistently; re-checked GitHub Actions workflows (no
#   pull_request_target usage; all four workflows carry explicit permissions
#   blocks); re-read packages/sdk/src/webhooks.ts (HMAC verification remains
#   fail-closed and timing-safe); re-checked security headers (next.config.ts:
#   HSTS, X-Frame-Options: DENY; CSP is nonce-based via proxy.ts, the
#   Next.js 16 successor to middleware.ts) and session cookie flags
#   (httpOnly always) remain intact; re-reviewed docs/compliance/GAP_ANALYSIS.md
#   — all open items (G-06 through G-27, e.g. ISMS/org gaps, backup restore
#   drill, vendor DPAs, API-key pepper, CSRF token, rate-limit Redis memory
#   fallback) remain accurately tracked as organizational/process gaps or
#   already-documented hardening recommendations, not confirmed code
#   vulnerabilities requiring a fix in this run.
# - npm audit (root, default and --omit=dev) + all 7 workspaces/packages
#   (packages/cli, packages/egress-proxy, packages/github-action,
#   packages/install, packages/mcp-audit, packages/mcp-runtime, packages/sdk)
#   and 5 example apps with their own package.json (enforcement-demo,
#   node-demo, site-guard-express, site-guard-nextjs, webhook-receiver):
#   0 vulnerabilities. Other examples under examples/ are dependency-free
#   snippets with no package.json.
# - Unit tests: 2847 passed, 78 skipped (223/224 files, root); 146 passed
#   (egress-proxy, install, github-action combined); 93 passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute:
#     MongoMemoryServer binary download blocked by this sandbox's outbound
#     network policy (fastdl.mongodb.org, HTTP 403); unrelated to source
#     changes and runs normally in GitHub Actions CI
#   - One transient timeout in test/managed-profile-policy.test.ts under
#     concurrent load (tsc+build+test+lint running together); re-ran the
#     file in isolation and the full suite standalone immediately after —
#     both clean (50/50 and 2847/2847 passing respectively) — confirmed a
#     resource-contention flake, not a regression
# - npx tsc --noEmit: clean
# - npm run build: clean (Next.js 16 / Turbopack production build succeeded)
# - npm run lint: pre-existing errors only (121 errors / 75 warnings, all in
#   already-committed code/tests, unchanged by this run); CI does not gate on
#   lint per AGENTS.md
# - No hardcoded secrets, private keys, or embedded-credential connection
#   strings found (grepped tracked source for API-key/token/password/secret
#   assignment patterns, AWS key IDs, PEM private-key headers, and
#   protocol://user:pass@ connection strings); only committed env file is
#   .env.example; sk-... string in test/mcp-audit.test.ts remains an
#   intentional test fixture; the two protocol://user:pass@ matches
#   (.github/workflows/ci.yml, scripts/dev/authenticated-shell-preview.mjs)
#   remain local/CI-only default credentials, not real secrets.

# Last audit: 2026-08-29 (full security + compliance audit)
# Result: PASSED (no new confirmed findings; no code changes since 2026-08-23)
# - No commits landed with source changes since the 84e6c33 "robots.txt sync"
#   commit reviewed in prior audits, so this pass re-validated the existing
#   state from source: re-verified the disabled-agent egress-authorization fix
#   (lib/egressAuthorize.ts: agentStatus === "disabled" check at the top of
#   authorizeEgressRequest()); re-checked GitHub Actions workflows (no
#   pull_request_target usage; all four workflows carry explicit permissions
#   blocks); re-checked security headers (next.config.ts: HSTS,
#   X-Frame-Options: DENY; CSP is nonce-based via proxy.ts) and session cookie
#   flags (httpOnly across lib/adminAuth.ts, lib/googleOAuth.ts, lib/reauth.ts,
#   lib/sessionCookies.ts) remain intact; re-reviewed
#   docs/compliance/GAP_ANALYSIS.md (G-01..G-27) — all open items remain
#   accurately tracked as organizational/process gaps (ISMS approval, backup
#   restore drill, vendor DPAs/subprocessor risk assessments, deploy
#   change-control) or already-documented hardening recommendations (API-key
#   pepper, CSRF token, rate-limit Redis memory fallback), not confirmed code
#   vulnerabilities requiring a fix in this run.
# - npm audit (root, default and --omit=dev) + all 7 workspace packages
#   (packages/cli, packages/egress-proxy, packages/github-action,
#   packages/install, packages/mcp-audit, packages/mcp-runtime, packages/sdk)
#   and 5 example apps with their own package.json (enforcement-demo,
#   node-demo, site-guard-express, site-guard-nextjs, webhook-receiver):
#   0 vulnerabilities.
# - Unit tests: 2847 passed, 78 skipped (223/224 files, root); 146 passed
#   (egress-proxy, install, github-action combined); 93 passed (mcp-runtime)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute:
#     MongoMemoryServer binary download blocked by this sandbox's outbound
#     network policy (fastdl.mongodb.org, HTTP 403); unrelated to source
#     changes and runs normally in GitHub Actions CI
#   - Note for future runs: running `npm install` directly inside a
#     packages/* workspace subdirectory (bypassing the root lockfile) can
#     desync the hoisted `detect-libc` resolution and transiently break
#     @parcel/watcher (used by tailwindcss/lightningcss) with a
#     "familySync is not a function" error in the build and in
#     test/cache-policy.test.ts. This is not a repository defect: a clean
#     `npm ci` from the repo root resolves `detect-libc` correctly (2.1.2,
#     with @tailwindcss/cli's own @parcel/watcher@2.5.1 nesting its
#     compatible 1.0.3), and the build and full test suite both pass clean
#     afterward. Workspace package tests should be run via `npm run
#     test:packages` (or `npm test -w <package>`) from the root, not via a
#     separate `cd <package> && npm install`.
# - npx tsc --noEmit: clean
# - npm run build: clean (Next.js 16 / Turbopack production build succeeded)
# - npm run lint: pre-existing errors only (121 errors / 75 warnings, all in
#   already-committed code/tests, unchanged by this run); CI does not gate on
#   lint per AGENTS.md
# - No hardcoded secrets, private keys, or embedded-credential connection
#   strings found (grepped tracked source for API-key/token/password/secret
#   assignment patterns, AWS key IDs, PEM private-key headers, and
#   protocol://user:pass@ connection strings); only committed env file is
#   .env.example; sk-... string in test/mcp-audit.test.ts remains an
#   intentional test fixture; the two protocol://user:pass@ matches
#   (.github/workflows/ci.yml, scripts/dev/authenticated-shell-preview.mjs)
#   remain local/CI-only default credentials, not real secrets.


# Last audit: 2026-08-30 (full security + compliance audit)
# Result: PASSED (no confirmed findings; reviewed the two source commits
#   landed since the 2026-08-29 audit)
# - Reviewed the only source changes since HEAD 7e64137 (2026-08-29 baseline):
#   0236d88 "feat(console): add Agent Orchestra SSO handoff" and 8c333a3
#   "fix(console): preserve Origin for Orchestra SSO handoff"
#   (app/console/orchestra/authorize/route.ts, lib/orchestraSso.ts,
#   lib/adminAuth.ts, lib/consoleAdmins.ts, app/console/login/page.tsx,
#   app/console/client.tsx, proxy.ts, next.config.ts). Findings:
#     - Assertions are EdDSA-signed JWTs with a 60s TTL, fixed
#       issuer/audience, and a server-generated jti; the signing key is read
#       from BEHALFID_ORCHESTRA_SSO_PRIVATE_KEY (server-only, never logged).
#     - GET /console/orchestra/authorize requires an admin-kind console
#       session (session.kind !== "admin" is rejected before any admin
#       lookup, so shared/legacy console sessions cannot mint assertions);
#       the admin is re-resolved live via findActiveConsoleAdmin so a
#       disabled/deleted admin cannot use a stale cookie to obtain one.
#     - state is validated against a strict base64url charset/length
#       pattern and rejected if duplicated or missing; the production
#       callback URL is hardcoded (ORCHESTRA_SSO_PRODUCTION_CALLBACK) and a
#       configured override is only honored outside production, and even
#       then only for a strictly-shaped http(s)://localhost/auth/callback
#       URL with no query/hash/userinfo — so a request-supplied callback or
#       returnUrl field cannot redirect the handoff (verified by test
#       "ignores arbitrary callback fields and pins the production
#       destination").
#     - Issuance fails closed: an admin-lookup error or a failed durable
#       audit write (recordAdminAuditStrict) both return 503 with no
#       assertion in the body, so the audit trail can't be silently skipped
#       on a security-sensitive issuance.
#     - The post-login "next" redirect target is constrained to strings
#       starting with "/console/orchestra/authorize?" (app/console/login/
#       page.tsx), so the login flow cannot be used as an open redirect via
#       this feature.
#     - The handoff document is served with its own strict CSP
#       (default-src 'none', nonced script/style, form-action pinned to the
#       resolved callback, frame-ancestors 'none', base-uri 'none'),
#       Cache-Control: no-store, private, and X-Content-Type-Options:
#       nosniff; proxy.ts special-cases this route so the app's normal
#       document CSP does not overwrite these headers. The 2026-08-28 fix
#       commit intentionally changed Referrer-Policy from no-referrer to
#       strict-origin (origin-only, not the full path) so the receiving
#       serv1.behalfid.com endpoint can apply its own Origin-based checks;
#       next.config.ts and the corresponding tests
#       (test/cache-policy.test.ts, test/orchestra-authorize-route.test.ts)
#       were updated in the same commit to match, so no stale assertion
#       remains.
#   No confirmed vulnerability found in this feature; no code change made.
# - npm audit (root, default and --omit=dev) + all 7 workspace packages
#   (packages/cli, packages/egress-proxy, packages/github-action,
#   packages/install, packages/mcp-audit, packages/mcp-runtime,
#   packages/sdk) and 5 example apps with their own package.json
#   (enforcement-demo, node-demo, site-guard-express, site-guard-nextjs,
#   webhook-receiver): 0 vulnerabilities.
# - Unit tests (npx vitest run): 2866 passed, 78 skipped (226 files, 1
#   failed suite)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute:
#     MongoMemoryServer binary download blocked by this sandbox's outbound
#     network policy (fastdl.mongodb.org, HTTP 403); unrelated to source
#     changes and runs normally in GitHub Actions CI, as in every prior
#     audit entry.
# - npx tsc --noEmit: clean
# - npm run build: clean (Next.js 16 / Turbopack production build
#   succeeded; /console/orchestra/authorize present in the route list)
# - npm run lint: 121 errors / 75 warnings, identical count to the prior
#   baseline and all in already-committed code/tests; CI does not gate on
#   lint per AGENTS.md
# - No hardcoded secrets, private keys, or embedded-credential connection
#   strings found (grepped tracked source for API-key/token/password/secret
#   assignment patterns, AWS key IDs, PEM private-key headers, and
#   protocol://user:pass@ connection strings); only committed env file is
#   .env.example; sk-... string in test/mcp-audit.test.ts remains an
#   intentional test fixture; the two protocol://user:pass@ matches
#   (.github/workflows/ci.yml, scripts/dev/authenticated-shell-preview.mjs)
#   remain local/CI-only default credentials, not real secrets. The three
#   new Orchestra SSO env vars added to .env.example
#   (BEHALFID_ORCHESTRA_SSO_PRIVATE_KEY/_KEY_ID/_CALLBACK_URL) are all
#   documented placeholders with empty values, not real key material.
# - Compliance: docs/compliance/GAP_ANALYSIS.md (G-01..G-27) reviewed; the
#   new SSO feature adds a durable, fail-closed admin audit trail
#   (orchestra_sso.issued) consistent with the existing G-05 control and
#   introduces no new gap. Remaining open items are unchanged:
#   organizational/process gaps (ISMS approval, backup restore drill,
#   vendor DPAs, deploy change-control) or already-documented hardening
#   recommendations (API-key pepper, CSRF token, rate-limit Redis memory
#   fallback), outside this run's safe-remediation scope.

# Last audit: 2026-08-31
# Result: PASSED (after a documentation fix; no code vulnerabilities found)
# - npm audit (root + --omit=dev): 0 vulnerabilities
# - npm audit: 0 vulnerabilities in packages/cli, packages/github-action, packages/install,
#   packages/mcp-audit, packages/mcp-runtime, packages/sdk, packages/egress-proxy
# - Unit tests: 2866 passed, 78 skipped (225/226 files)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute:
#     MongoMemoryServer binary download blocked by this sandbox's outbound
#     network policy (fastdl.mongodb.org, HTTP 403); unrelated to source
#     changes and runs normally in GitHub Actions CI, as in every prior
#     audit entry.
# - npx tsc --noEmit: clean
# - npm run build: clean (Next.js production build succeeded)
# - npm run api:inventory:check: docs/API_TEST_MATRIX.md up to date
# - npm run test:api-contracts: 650 passed
# - npm run lint: 121 errors / 75 warnings, identical to the prior baseline,
#   all in already-committed code/tests; not a CI gate (no lint step in
#   .github/workflows/ci.yml)
# - No hardcoded secrets, private keys, or embedded-credential connection
#   strings found (dedicated grep sweep of all 1,684 tracked non-vendor
#   files for API-key/token/password/secret literals, AWS key IDs, PEM
#   private-key headers, and protocol://user:pass@ strings); only committed
#   env files are .env.example / examples/site-guard-demo/.env.example,
#   both placeholders only.
# - Targeted manual review of the most recently changed security-sensitive
#   surface (Agent Orchestra SSO handoff: app/console/orchestra/authorize/route.ts,
#   lib/orchestraSso.ts, lib/adminAuth.ts, lib/consoleAdmins.ts): callback
#   URL is server-pinned (not client-controlled) so no open redirect; the
#   Referrer-Policy change in the prior commit only affects the outbound
#   header, no client-supplied Origin/Referer is trusted for a security
#   decision; admin session is HMAC-verified and re-checked live against
#   the database before issuing an assertion; the EdDSA assertion is
#   short-lived (60s), scoped, and delivered only via POST body. No
#   confirmed Critical/High vulnerability. One defense-in-depth hardening
#   idea was considered (checking Sec-Fetch-Site on the GET before minting
#   the assertion) but not applied: it is not a demonstrated exploit (state
#   binding is conventionally the relying party's responsibility in this
#   OAuth-style handoff), and blocking cross-subdomain "same-site" fetches
#   without knowing Orchestra's actual deployment domain risks breaking a
#   legitimate flow — left as a recommendation instead of a speculative
#   change to production auth behavior.
# - Also grepped app/, lib/, and packages/ source (excluding tests) for
#   eval/new Function/exec with untrusted input, raw SQL string building,
#   dangerouslySetInnerHTML with user input, unvalidated redirects, SSRF via
#   user-controlled fetch URLs, Math.random() used for tokens, permissive
#   CORS, and missing cookie flags — no confirmed issues; existing guards
#   (safeOAuthNextPath, validatePublicUrl SSRF check, parameterized drizzle
#   queries, consistent httpOnly/sameSite/secure cookie helpers) already
#   cover these classes.
# - Compliance documentation fix: docs/compliance/GAP_ANALYSIS.md section 3
#   still listed "Public disclosure accuracy | Missing | Critical" even
#   though section 2 (G-21/G-22) already logged that exact issue as
#   REMEDIATED on 2026-08-12. Verified against the live pages
#   (app/privacy/page.tsx, app/compliance/page.tsx: both correctly disclose
#   Supabase (Postgres) as the datastore and HeyCatch as consent-gated
#   analytics) and corrected the stale/contradictory table row to match.
#   No other code-level compliance defects found; remaining GAP_ANALYSIS.md
#   items are organizational/process gaps (ISMS approval, vendor DPAs,
#   backup restore drill, deploy change-control) outside repository scope,
#   or already-tracked hardening recommendations (G-10 API-key pepper,
#   G-17 CSRF token, G-15 rate-limit Redis fallback).

# Last audit: 2026-09-01
# Result: PASSED (after a dependency fix; no new source changes since the
#   2026-08-31 audit — HEAD was still 8c333a3 plus only audit-log/doc
#   commits)
# - npm audit (root, default): found 1 high-severity vulnerability newly
#   disclosed since yesterday's run: browserslist <=4.28.6 (GHSA-c83g-rgw3-j3cx
#   unbounded memory growth/DoS via cache eviction, GHSA-73wf-gq98-2v4g
#   uncaught crash/prototype write via untrusted browserslist-stats.json),
#   pulled in transitively via @sentry/nextjs -> @sentry/bundler-plugin-core
#   -> @babel/core and @sentry/webpack-plugin -> webpack. Fixed by adding a
#   "browserslist": "^4.28.8" entry to package.json overrides (same pattern
#   as the existing postcss/sharp/undici/@swc/helpers overrides), then
#   `npm install` to resolve and rebuild node_modules/package-lock.json.
#   Re-ran `npm audit` (root, default and --omit=dev) after the fix: 0
#   vulnerabilities. Confirmed via `npm ls browserslist` that all three
#   resolution paths now dedupe/override to 4.28.8.
# - npm audit: 0 vulnerabilities in packages/cli, packages/egress-proxy,
#   packages/github-action, packages/install, packages/mcp-audit,
#   packages/mcp-runtime, packages/sdk, examples/enforcement-demo,
#   examples/node-demo, examples/site-guard-express,
#   examples/site-guard-nextjs, examples/webhook-receiver.
# - Unit tests (npx vitest run): 2866 passed, 78 skipped (225/226 files)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute:
#     MongoMemoryServer binary download blocked by this sandbox's outbound
#     network policy (fastdl.mongodb.org, HTTP 403); unrelated to source
#     changes and runs normally in GitHub Actions CI, as in every prior
#     audit entry.
# - npx tsc --noEmit: clean
# - npm run build: clean (Next.js 16 / Turbopack production build
#   succeeded; full route list including /console/orchestra/authorize
#   present)
# - npm run api:inventory:check: docs/API_TEST_MATRIX.md up to date
# - npm run test:api-contracts: 650 passed
# - npm run lint: 121 errors / 75 warnings, identical to the prior
#   baseline, all in already-committed code/tests; not a CI gate (no lint
#   step in .github/workflows/ci.yml)
# - No hardcoded secrets, private keys, or embedded-credential connection
#   strings found (grep sweep of tracked source for API-key/token/
#   password/secret literals, AWS key IDs, PEM private-key headers, and
#   protocol://user:pass@ strings). The three matches found were all
#   confirmed non-issues on inspection: CSS custom-property names
#   ("--text-secondary" etc.) in app/design-system/foundation/page.tsx, a
#   detection regex literal (not a key) in test/api-route-contracts.test.ts,
#   and .github/workflows/cli-release.yml's git-clone URL which
#   interpolates ${TAP_TOKEN} sourced from `secrets.HOMEBREW_TAP_TOKEN`,
#   not a literal credential. Only committed env files remain
#   .env.example / examples/site-guard-demo/.env.example, both placeholder
#   only.
# - Re-checked all four GitHub Actions workflows (ci.yml, cli-release.yml,
#   package-release.yml, sdk-release.yml): each still declares an explicit,
#   minimal top-level `permissions:` block; no `pull_request_target` usage
#   found anywhere in .github/workflows/.
# - Re-verified docs/compliance/GAP_ANALYSIS.md against the live
#   /privacy and /compliance pages: both still correctly disclose Supabase
#   (Postgres) as the datastore and HeyCatch as consent-gated analytics,
#   consistent with the 2026-08-31 fix; no new contradiction found.
#   Remaining open items are unchanged: organizational/process gaps (ISMS
#   approval, vendor DPAs, backup restore drill, deploy change-control) or
#   already-tracked hardening recommendations (G-10 API-key pepper, G-17
#   CSRF token, G-15 rate-limit Redis fallback), outside this run's
#   safe-remediation scope.
# - No authorization/business-logic/business-logic-adjacent source changed
#   since the 2026-08-31 audit, so no new manual review of that surface was
#   needed this run.

# Last audit: 2026-09-02
# Result: PASSED (after two dependency fixes; no new source changes since
#   the 2026-08-31 audit — HEAD was still 8c333a3 plus only audit-log/doc
#   and dependency-override commits)
# - npm audit (root, default): found 1 high-severity vulnerability newly
#   disclosed since yesterday's run: fast-uri 3.0.0-3.1.5 (GHSA-5jgf-p345-68v8
#   host confusion via skipped IDN canonicalization, GHSA-f65p-4m7j-42xc SSRF
#   via malformed IPv6 normalization, GHSA-fph4-wmhf-6fwf SSRF via repeated
#   hostname percent-decoding, GHSA-jqff-g426-hqxp host confusion via
#   percent-encoded scheme normalization), pulled in transitively via ajv
#   (used by webpack's schema-utils, part of the @sentry/webpack-plugin /
#   @sentry/bundler-plugin-core build tooling chain). Root cause: the
#   existing package.json override pinned "fast-uri": "^3.1.5", i.e. exactly
#   the top of the now-vulnerable range, so as soon as 3.1.5 was retroactively
#   flagged the override itself was the stale/vulnerable pin. Fixed by
#   bumping the override to "^3.1.7" (latest 3.x release, still satisfies
#   ajv@8.20.0's own "^3.0.1" dependency range so no unintended major bump).
#   Re-ran `npm audit` (root, default and --omit=dev) after the fix: 0
#   vulnerabilities. Confirmed via `npm ls fast-uri --all` that both
#   resolution paths now dedupe/override to 3.1.7.
# - npm audit: 0 vulnerabilities in packages/cli, packages/egress-proxy,
#   packages/github-action, packages/install, packages/mcp-audit,
#   packages/mcp-runtime, packages/sdk, examples/enforcement-demo,
#   examples/node-demo, examples/site-guard-nextjs, examples/webhook-receiver.
#   examples/site-guard-express: found 3 moderate-severity vulnerabilities
#   (qs array-limit bypass GHSA-x5fp-wj9c-mxmx, qs DoS via attacker-controlled
#   isBuffer GHSA-4mjr-xmp4-gh2g, inherited into body-parser and express).
#   express@4.22.2 pins body-parser ~1.20.5, which pins qs ~6.15.1 — the
#   entire vulnerable range (2.2.5-6.15.3) is reachable through express 4.x's
#   own declared dependency tree, so `npm audit fix` could not resolve it
#   automatically. Fixed by adding a local "overrides": { "qs": "^6.16.0" }
#   to examples/site-guard-express/package.json (same override pattern used
#   at the repo root), then `npm install` and `npm run build` (tsc) to
#   confirm the example still compiles against the overridden qs. Re-ran
#   `npm audit` after the fix: 0 vulnerabilities. This is example/demo code,
#   not part of the deployed application, but is still tracked and audited
#   in-repo.
# - Unit tests (npx vitest run): 2866 passed, 78 skipped (225/226 files)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute:
#     MongoMemoryServer binary download blocked by this sandbox's outbound
#     network policy (fastdl.mongodb.org, HTTP 403); unrelated to source
#     changes and runs normally in GitHub Actions CI, as in every prior
#     audit entry.
# - npx tsc --noEmit: clean
# - npm run build: clean (Next.js 16 / Turbopack production build succeeded)
# - npm run api:inventory:check: docs/API_TEST_MATRIX.md up to date
# - npm run test:api-contracts: 650 passed
# - npm run lint: 121 errors / 75 warnings, identical to the prior baseline,
#   all in already-committed code/tests; not a CI gate (no lint step in
#   .github/workflows/ci.yml)
# - No hardcoded secrets, private keys, or embedded-credential connection
#   strings found (grep sweep of tracked source for API-key/token/
#   password/secret literals, AWS key IDs, PEM private-key headers, and
#   protocol://user:pass@ strings). Matches found were the same known
#   non-issues as every prior audit: CSS custom-property names in
#   app/design-system/foundation/page.tsx, and local/CI-only default
#   credentials (postgres:postgres@127.0.0.1, behalf:behalf@localhost) in
#   AGENTS.md, docs/POSTGRES_SCHEMA.md, .github/workflows/ci.yml, and
#   scripts/dev/authenticated-shell-preview.mjs. Only committed env files
#   remain .env.example / examples/site-guard-demo/.env.example, both
#   placeholder only.
# - Re-checked all four GitHub Actions workflows (ci.yml, cli-release.yml,
#   package-release.yml, sdk-release.yml): each still declares an explicit,
#   minimal top-level `permissions:` block; no `pull_request_target` usage
#   found anywhere in .github/workflows/.
# - No authorization/business-logic/business-logic-adjacent source changed
#   since the 2026-08-31 audit (diff from 8c333a3 to this run's HEAD was
#   limited to docs/compliance/GAP_ANALYSIS.md, package.json/-lock.json, and
#   this file), so no new manual review of that surface was needed this run.

# Last audit: 2026-09-03 (full security + compliance audit)
# Result: PASSED (no new confirmed findings; no source changes since the
#   2026-08-31 audit — HEAD was still 8c333a3 plus only audit-log/dependency-
#   override commits)
# - npm audit (root, default and --omit=dev): 0 vulnerabilities. The default
#   "quick" audit endpoint returned a transient 503, then a 400 ("Invalid
#   package tree") after that — both are the npmjs.org registry's ongoing
#   retirement of that endpoint, not a repo defect; a fresh
#   `npm install --package-lock-only` followed by `npm audit` came back clean.
# - npm audit --omit=dev: 0 vulnerabilities in all 7 workspace packages
#   (packages/cli, packages/egress-proxy, packages/github-action,
#   packages/install, packages/mcp-audit, packages/mcp-runtime, packages/sdk)
#   and all 5 example apps with their own package.json (enforcement-demo,
#   node-demo, site-guard-express, site-guard-nextjs, webhook-receiver);
#   several hit the same transient registry 503/400 and came back clean on
#   retry.
# - Unit tests (npx vitest run): 2866 passed, 78 skipped (225/226 files)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute:
#     MongoMemoryServer binary download blocked by this sandbox's outbound
#     network policy (fastdl.mongodb.org, HTTP 403); unrelated to source
#     changes and runs normally in GitHub Actions CI, as in every prior
#     audit entry.
# - npx tsc --noEmit: clean
# - npm run build: clean (Next.js 16 / Turbopack production build succeeded)
# - npm run api:inventory:check: docs/API_TEST_MATRIX.md up to date
# - npm run test:api-contracts: 650 passed
# - npm run lint: 121 errors / 75 warnings, identical to the prior baseline,
#   all in already-committed code/tests; not a CI gate (no lint step in
#   .github/workflows/ci.yml)
# - No hardcoded secrets, private keys, or embedded-credential connection
#   strings found (grep sweep of tracked source for API-key/token/
#   password/secret literals, AWS key IDs, PEM private-key headers, and
#   protocol://user:pass@ strings). Matches found were the same known
#   non-issues as every prior audit: CSS custom-property names in
#   app/design-system/foundation/page.tsx, and local/CI-only default
#   credentials (postgres:postgres@127.0.0.1, behalf:behalf@localhost) in
#   AGENTS.md, docs/POSTGRES_SCHEMA.md, .github/workflows/ci.yml, and
#   scripts/dev/authenticated-shell-preview.mjs; the sk-... string in
#   test/mcp-audit.test.ts remains an intentional test fixture. Only
#   committed env files remain .env.example / examples/site-guard-demo/
#   .env.example, both placeholder only.
# - Re-checked all four GitHub Actions workflows (ci.yml, cli-release.yml,
#   package-release.yml, sdk-release.yml): each still declares an explicit,
#   minimal top-level `permissions:` block; no `pull_request_target` usage
#   found anywhere in .github/workflows/.
# - Re-verified the disabled-agent egress-authorization fix
#   (lib/egressAuthorize.ts: agentStatus === "disabled" check at the top of
#   authorizeEgressRequest()) is still in place.
# - No authorization/business-logic/business-logic-adjacent source changed
#   since the 2026-08-31 audit, so no new manual review of that surface was
#   needed this run.

# Last audit: 2026-09-04 (full security + compliance audit)
# Result: PASSED (no new confirmed findings; no source changes since the
#   2026-08-31 audit — HEAD was still 8c333a3 plus only audit-log/dependency-
#   override commits)
# - npm audit (root, default and --omit=dev): 0 vulnerabilities.
# - npm audit --omit=dev: 0 vulnerabilities in all 7 workspace packages
#   (packages/cli, packages/egress-proxy, packages/github-action,
#   packages/install, packages/mcp-audit, packages/mcp-runtime, packages/sdk)
#   and all 5 example apps with their own package.json (enforcement-demo,
#   node-demo, site-guard-express, site-guard-nextjs, webhook-receiver).
# - Unit tests (npx vitest run): 2866 passed, 78 skipped (225/226 files)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute:
#     MongoMemoryServer binary download blocked by this sandbox's outbound
#     network policy (fastdl.mongodb.org, HTTP 403); unrelated to source
#     changes and runs normally in GitHub Actions CI, as in every prior
#     audit entry.
# - npx tsc --noEmit: clean
# - npm run build: clean (Next.js 16 / Turbopack production build succeeded)
# - npm run api:inventory:check: docs/API_TEST_MATRIX.md up to date
# - npm run test:api-contracts: 650 passed
# - npm run lint: 121 errors / 75 warnings, identical to the prior baseline,
#   all in already-committed code/tests; not a CI gate (no lint step in
#   .github/workflows/ci.yml)
# - No hardcoded secrets, private keys, or embedded-credential connection
#   strings found (grep sweep of tracked source for API-key/token/
#   password/secret literals, AWS key IDs, PEM private-key headers, and
#   protocol://user:pass@ strings). Matches found were the same known
#   non-issues as every prior audit: local/CI-only default credentials
#   (postgres:postgres@127.0.0.1, behalf:behalf@localhost) in AGENTS.md,
#   docs/POSTGRES_SCHEMA.md, .github/workflows/ci.yml, and
#   scripts/dev/authenticated-shell-preview.mjs. Only committed env files
#   remain .env.example / examples/site-guard-demo/.env.example, both
#   placeholder only.
# - Re-checked all four GitHub Actions workflows (ci.yml, cli-release.yml,
#   package-release.yml, sdk-release.yml): each still declares an explicit,
#   minimal top-level `permissions:` block; no `pull_request_target` usage
#   found anywhere in .github/workflows/.
# - Re-verified the disabled-agent egress-authorization fix
#   (lib/egressAuthorize.ts: agentStatus === "disabled" check at the top of
#   authorizeEgressRequest()) is still in place.
# - Re-verified docs/compliance/GAP_ANALYSIS.md against the live /privacy
#   and /compliance pages: both still correctly disclose Supabase (Postgres)
#   as the datastore and HeyCatch as consent-gated analytics; no new
#   contradiction found. Remaining open items are unchanged: organizational/
#   process gaps (ISMS approval, vendor DPAs, backup restore drill, deploy
#   change-control) or already-tracked hardening recommendations (G-10
#   API-key pepper, G-17 CSRF token, G-15 rate-limit Redis fallback),
#   outside this run's safe-remediation scope.
# - No authorization/business-logic/business-logic-adjacent source changed
#   since the 2026-08-31 audit, so no new manual review of that surface was
#   needed this run.

# Last audit: 2026-09-05 (full security + compliance audit)
# Result: PASSED (no new confirmed findings; no source changes since the
#   2026-08-31 audit — HEAD was still 8c333a3 plus only audit-log/dependency-
#   override commits)
# - npm audit (root, default and --omit=dev): 0 vulnerabilities.
# - npm audit --omit=dev: 0 vulnerabilities in all 7 workspace packages
#   (packages/cli, packages/egress-proxy, packages/github-action,
#   packages/install, packages/mcp-audit, packages/mcp-runtime, packages/sdk)
#   and all 5 example apps with their own package.json (enforcement-demo,
#   node-demo, site-guard-express, site-guard-nextjs, webhook-receiver).
# - Unit tests (npx vitest run): 2866 passed, 78 skipped (225/226 files)
#   - test/mongo-repository-contracts.test.ts (73 tests) could not execute:
#     MongoMemoryServer binary download blocked by this sandbox's outbound
#     network policy (fastdl.mongodb.org, HTTP 403); unrelated to source
#     changes and runs normally in GitHub Actions CI, as in every prior
#     audit entry.
# - npx tsc --noEmit: clean
# - npm run build: clean (Next.js 16 / Turbopack production build succeeded)
# - npm run api:inventory:check: docs/API_TEST_MATRIX.md up to date
# - npm run test:api-contracts: 650 passed
# - npm run lint: 121 errors / 75 warnings, identical to the prior baseline,
#   all in already-committed code/tests; not a CI gate (no lint step in
#   .github/workflows/ci.yml)
# - No hardcoded secrets, private keys, or embedded-credential connection
#   strings found (grep sweep of tracked source for API-key/token/
#   password/secret literals, AWS key IDs, PEM private-key headers, and
#   protocol://user:pass@ strings). Matches found were the same known
#   non-issues as every prior audit: local/CI-only default credentials
#   (postgres:postgres@127.0.0.1, behalf:behalf@localhost) in AGENTS.md,
#   docs/POSTGRES_SCHEMA.md, .github/workflows/ci.yml, and
#   scripts/dev/authenticated-shell-preview.mjs. Only committed env files
#   remain .env.example / examples/site-guard-demo/.env.example, both
#   placeholder only.
# - Re-checked all four GitHub Actions workflows (ci.yml, cli-release.yml,
#   package-release.yml, sdk-release.yml): each still declares an explicit,
#   minimal top-level `permissions:` block; no `pull_request_target` usage
#   found anywhere in .github/workflows/.
# - Re-verified the disabled-agent egress-authorization fix
#   (lib/egressAuthorize.ts: agentStatus === "disabled" check at the top of
#   authorizeEgressRequest()) is still in place.
# - Re-verified docs/compliance/GAP_ANALYSIS.md against the live /privacy
#   and /compliance pages: both still correctly disclose Supabase (Postgres)
#   as the datastore and HeyCatch as consent-gated analytics; no new
#   contradiction found. Remaining open items are unchanged: organizational/
#   process gaps (ISMS approval, vendor DPAs, backup restore drill, deploy
#   change-control) or already-tracked hardening recommendations (G-10
#   API-key pepper, G-17 CSRF token, G-15 rate-limit Redis fallback),
#   outside this run's safe-remediation scope.
# - No authorization/business-logic/business-logic-adjacent source changed
#   since the 2026-08-31 audit, so no new manual review of that surface was
#   needed this run.
